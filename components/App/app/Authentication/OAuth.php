<?php declare (strict_types=1);

namespace App\Authentication;

class OAuth
{

    const TOKEN_CUSTOM_PROPERTIES_PROVIDER = 'token';
    const USER_VALIDATOR = 'user_validator';
    const SCOPE_VALIDATOR = 'scope_validator';
}
